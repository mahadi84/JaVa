
=========Find number that value is/are below 0(Zero)============

        List<Integer> list = new ArrayList<>();
        list.add(-6);
        list.add(-5);
        list.add(0);
        list.add(5);
        print(list, e -> e < 0);
        }
        public static void print(List<Integer> list, Predicate<Integer> p) {
        for (Integer num : list)
        if (p.test(num))
        System.out.println(num);

 OUTPUT: -6, -5
